*.sh text eol=lf
# cmd.exe misparses labels/gotos in LF-only batch files; PowerShell is fine
# either way but is kept CRLF alongside for a consistent Windows checkout.
*.bat text eol=crlf
*.ps1 text eol=crlf
